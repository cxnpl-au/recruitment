import React from "react";

// Import components
import LoginForm from "../components/LoginForm";

const Login = () => {
  return (
    <main>
      {/* Page title */}
      <h2>Log in</h2>
      {/* Form for logging in */}
      <LoginForm />
    </main>
  );
};

export default Login;
